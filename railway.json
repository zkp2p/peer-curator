{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node dist/cli.js sync",
    "cronSchedule": "0 0 * * *",
    "restartPolicyType": "NEVER"
  }
}
